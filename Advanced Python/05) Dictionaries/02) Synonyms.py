# Read the number of synonyms
count = int(input())
# Create a dictionary to store the synonyms
synonymsDict = dict()
# Iterate through the number of synonyms

    # Read the two synonyms
    
    # Add the synonyms to the dictionary
    
    # Add the synonyms to the dictionary in reverse
    
    
# Read the word to find the synonyms for

# Print the synonyms for the word



'''
count = int(input())
synonymsDict = dict()

for i in range(count):
  word1, word2 = input().split()
  synonymsDict[word1] = word2
  synonymsDict[word2] = word1

word = input()
print(synonymsDict[word], end="")
'''