import java.util.Scanner;

class Main {
  public static void main(String[] args) {
    // Create scanner and store it in a new string variable called input
    

    // Create a for loop that iterates through the input
    for () {
      // Check if the character is a vowel (HINT: use the charAt() method and the == operator)
      if () {
        // Print all the vowels on the same line
        
      }
    }
  }
}

/*
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
      Scanner sc = new Scanner(System.in);
      String input = sc.nextLine();
      for (int i = 0; i < input.length(); i++) {
        if (input.charAt(i) == 'a' || input.charAt(i) == 'e' || input.charAt(i) == 'i' || input.charAt(i) == 'o' || input.charAt(i) == 'u') {
          System.out.print(input.charAt(i));
        }
      }
    }
  }
*/