/*
Ask the user how many names they would like to enter. 
Build an array that is composed of all the names entered. 
Print out the first letter of each name, all on one line, with no spaces. 
*/

// import scanner


class Main {
  public static void main(String[] args) {
    // Create a new scanner object
    
    // Ask the user how many names they would like to enter
    
    // Consume the newline left after nextInt()
    
    // Create a new array and repeatedly ask the user to enter names (the number of times that they wanted to):
    
    // Create a for loop that iterates through the array and asks the user to enter the names
    
    /* Loop through the array and print out the first letter of each name 
    (use print instead of println), making sure there's no space between them. */
    
  
  }
  }

  /*
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
      Scanner sc = new Scanner(System.in);
      int num = sc.nextInt();
      sc.nextLine();
      String[] names = new String[num];
      for(int i = 0; i < num; i++) {
        names[i] = sc.nextLine();
      }
      for(int i = 0; i < names.length; i++) {
        System.out.print(names[i].charAt(0));
      }
    }
  }
   * 
   */