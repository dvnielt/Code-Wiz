/* 
 * The code provided in your main method will take in five integers and save them into an array called arr. 
 * Print out each element of the array multiplied by the element after it. 
 * For the final element multiply it by the first element.
 */

 // import scanner


 class Main {
  public static void main(String[] args) {
    // Create a new scanner object
    
    // Create a new array called arr
    
    // Create a for loop that iterates through the array and asks the user to enter the numbers
    
    // Create a for loop that iterates through the array and prints out each element multiplied by the element after it
    
    // For the final element multiply it by the first element
    
  }
}

/*
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
      Scanner sc = new Scanner(System.in);
      int[] arr = new int[5];
      for(int i = 0; i < 5; i++) {
        arr[i] = sc.nextInt();
      }
      for(int i = 0; i < arr.length - 1; i++) {
        System.out.println(arr[i] * arr[i + 1]);
      }
      System.out.println(arr[arr.length - 1] * arr[0]);
    }
  }
 */