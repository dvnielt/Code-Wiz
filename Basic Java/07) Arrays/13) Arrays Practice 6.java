/*
 * Take in 5 integers and store them in an array
 * Print out the largest value in the array
 * Print out the smallest value in the array
 * Hint: You'll need to use a for loop to iterate through the array and a conditional statement to find the largest and smallest values.
 */

 // import scanner


 class Main {
    public static void main(String[] args) {
      // Create a new scanner object
      
      // Create a new array called arr
      
      // Create a for loop that iterates through the array and asks the user to enter the numbers
      
      // Create a variable called largest and smallest and set them to the first element of the array
      
      // Create a for loop that iterates through the array and finds the largest and smallest values
      
        // If the current element is greater than the largest, set largest to the current element
        
        // If the current element is less than the smallest, set smallest to the current element
        
      // Print out the largest and smallest values
      
    }
 }

 /*
   import java.util.Scanner;
   class Main {
     public static void main(String[] args) {
       Scanner sc = new Scanner(System.in);
     int[] arr = new int[5];
       for(int i = 0; i < 5; i++) {
         arr[i] = sc.nextInt();
       }
     }
     int largest = arr[0];
     int smallest = arr[0];
     for(int i = 0; i < 5; i++) {
       if(arr[i] > largest) {
         largest = arr[i];
       }
       if(arr[i] < smallest) {
        smallest = arr[i];
       }
     }
     System.out.println(largest);
     System.out.println(smallest);
   }
  */