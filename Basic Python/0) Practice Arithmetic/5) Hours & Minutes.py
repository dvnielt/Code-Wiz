# Read an integer for the seconds passed since midnight:
seconds = int(input())

# How many total seconds are there in an hour? 
# 60 seconds per minute * 60 minutes per hour = 3600 seconds
hours_passed_midnight = seconds // 3600

# Calculate how many seconds have passed since midnight:
minutes_passed_midnight = 

# Print the hours and minutes:
print(str() + " " + str())