# We've added the factorial function from the last lesson
def factorial(number):
    # Create a product variable and set it to 1
    product = 1
    # Loop through all the numbers from 1 to number + 1     HINT: range(1, number+1)
    for num in range(1, number+1):
        # Multiply the product variable by the number in the loop
        product *= num
    # Return the product variable
    return product
    
# Create a Total variable and set it to 0
Total = 

# Read an integer for N
N = 

# Make a for loop that repeats N times


    # On each loop, get an integer input


    # Use the factorial function to get the factorial of that number


    # Add that factorial to the Total variable


# Print the Final Total

