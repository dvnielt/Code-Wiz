# Read an integer for the number of levels
Levels = int(input())

# Loop through each level from 1 to Levels  HINT: range(1, Levels + 1)
for level in range(1, Levels + 1):


    # On each level, Loop through from 1 to the level number  HINT: range(1, level + 1)
    for i in range(1, level + 1):

        # Print the number with a space at the end (end=" ")
        
    
    # Add a new line after each level with print()

