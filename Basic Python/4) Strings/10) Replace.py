"""
for index in range(len(string)):   - loop through each character
    string[index]                      - get the character at a position
    print(..., end="")                 - print without starting a new line
"""

# Read a string:
string = input()

# Loop over each character in the string by index
    # If the character is "1"
        # print "one" instead
    # Otherwise
        # print the character as it is
