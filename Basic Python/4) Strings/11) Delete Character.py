"""
index % 3 == 0   - true when the index is divisible by 3
"""

# Read a string:
string = input()

# Loop over each character in the string by index
    # If the index is divisible by 3
        # skip it (don’t print)
    # Otherwise
        # print the character
