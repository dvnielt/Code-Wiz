'''
word.find("A") - first index of an "A" in the string 
word.rfind("A") - Same as string.find("A") in reverse 
word.find("A", 1) - first index of an "A" in the string starting at index 1 (instead of starting at index 0) 
len(word) - how many characters are in the string
word.count("f") - how many "f" in the string
'''

# Read a string:
string = input()

# Count the number of occurences of the letter "f" 
count = string.count("f")

# if f is not in the string
    
    # print -1


# else if f shows up once in the string
    
    # print the first index of it using find()


# else
    # find the first index using find()

    # find the last index using rfind()

    # print the first and last index

