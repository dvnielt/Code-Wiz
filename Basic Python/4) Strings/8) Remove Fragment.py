"""
word.find("h")   - first index of "h"
word.rfind("h")  - last index of "h"
word.replace("h", "H") - replace "h" with "H"
"""
# Read a string:
string = input()

# Find the index of the first "h"
# Find the index of the last "h"

# Slice into 3 parts:
#   - everything before the first "h"
#   - everything from the first "h" through the last "h"
#   - everything after the last "h"

# Replace all "h" with "H" in the middle part

# Print all the pieces joined back together
